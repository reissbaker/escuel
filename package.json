{
  "name": "escuel",
  "version": "0.0.1",
  "description": "sql queries",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build/ && ./node_modules/.bin/babel --out-dir build/ --presets es2015 lib/"
  },
  "keywords": [
    "sql"
  ],
  "author": "Matt Baker",
  "license": "ISC",
  "dependencies": {
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "babel-cli": "^6.1.18",
    "babel-preset-es2015": "^6.1.18"
  }
}
